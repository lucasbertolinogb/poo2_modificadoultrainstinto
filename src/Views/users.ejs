<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" type="text/css" href="style.css">
    <style>
        body {
            font-family: Arial, sans-serif;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        table {
            width: 80%;
            margin: 20px auto;
            border-collapse: collapse;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
        }

        th, td {
            padding: 10px;
            border: 1px solid #ddd;
            text-align: left;
        }

        thead {
            background-color: #f2f2f2;
        }

        tbody tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        /* Adicione um pouco de espaço extra nas células da coluna de Tipo de Usuário */
        td:first-child {
            padding-left: 20px;
            padding-right: 20px;
        }
    </style>
</head>
<body>
    <h1>Lista de Usuarios</h1>
    <table>
        <thead>
            <th>Tipo Usuario</th>
            <th>Nome</th>
            <th>Email</th>
        </thead>
        <tbody>
            <% for(var i = 0; i < users.length; i++){ %>
                <tr>
                    <td> 
                        <% if (users[i].tipoUsuario === 0) { %>
                            Aluno
                        <% } else if (users[i].tipoUsuario === 1) { %>
                            Professor
                        <% } else { %> 
                        Tipo Desconhecido
                        <% } %>
                    </td>    
                    <td><%= users[i].nome %></td>
                    <td><%= users[i].email %></td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>